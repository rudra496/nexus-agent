name: Welcome New Contributors

on:
  pull_request_target:
    types: [opened]
  issues:
    types: [opened]

jobs:
  welcome:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write
    steps:
      - uses: actions/first-interaction@v1
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          issue-message: >
            Welcome to Nexus Agent! Thanks for opening this issue.
            We'll review it as soon as possible.
          pr-message: >
            Welcome to Nexus Agent! Thanks for your first pull request!
            We'll review your changes soon.
